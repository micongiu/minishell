#include "../minishell.h"

void	ft_execute_pipe_line(t_env_var *env, t_process_list *head)
{
	int i = 0;
	char **mat;

	while(env)
	{
		i++;
		env->next;
	}
	mat = malloc()
	// conta i nodi
	// per ogni nodo malloca
	// conta il nome e il valore
	// append nome + valore
	// NULL;
}
